fire (or fire <direction number>) starts to fire until fire_stop; to fire only once you can use <direction command> f.
For example: "fire 1" starts to fire northeast until "fire_stop". "northeast f" fires once to northeast.
Use "stay f" to target on yourself.
You can turn off fire with fire_stop.
